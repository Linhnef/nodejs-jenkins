version: "3.9"
services:
    api:
        build:
            context: .
        ports:
            - "8000:8000"
    nginx:
        restart: always
        build:
            context: ./nginx
        ports:
            - '80:80'
